# secrets CLI

Zero-dependency CLI over the `secrets` resource pack (generated by
[agentic-anything](https://github.com/THUQiXuan/agentic-anything)).

```bash
python secrets_cli.py pages
python secrets_cli.py page <page_id>
python secrets_cli.py --json search <keywords>
python secrets_cli.py apis
python secrets_cli.py fetch /some/path      # live same-origin GET
python secrets_cli.py form-curl 0           # curl template for form #0
```

All commands accept `--json` for machine-readable output.
Exit codes: 0 = success, 1 = error (message on stderr).
