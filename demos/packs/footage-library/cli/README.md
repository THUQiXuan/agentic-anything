# footage-library CLI

Zero-dependency CLI over the `footage-library` resource pack (generated by
[agentic-anything](https://github.com/THUQiXuan/agentic-anything)).

```bash
python footage_library_cli.py pages
python footage_library_cli.py page <page_id>
python footage_library_cli.py --json search <keywords>
python footage_library_cli.py apis
python footage_library_cli.py fetch /some/path      # live same-origin GET
python footage_library_cli.py form-curl 0           # curl template for form #0
```

All commands accept `--json` for machine-readable output.
Exit codes: 0 = success, 1 = error (message on stderr).
