# website CLI

Zero-dependency CLI over the `website` resource pack (generated by
[agentic-anything](https://github.com/THUQiXuan/agentic-anything)).

```bash
python website_cli.py pages
python website_cli.py page <page_id>
python website_cli.py --json search <keywords>
python website_cli.py apis
python website_cli.py fetch /some/path      # live same-origin GET
python website_cli.py form-curl 0           # curl template for form #0
```

All commands accept `--json` for machine-readable output.
Exit codes: 0 = success, 1 = error (message on stderr).
