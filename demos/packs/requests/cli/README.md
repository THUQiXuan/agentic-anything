# requests CLI

Zero-dependency CLI over the `requests` resource pack (generated by
[agentic-anything](https://github.com/THUQiXuan/agentic-anything)).

```bash
python requests_cli.py pages
python requests_cli.py page <page_id>
python requests_cli.py --json search <keywords>
python requests_cli.py apis
python requests_cli.py fetch /some/path      # live same-origin GET
python requests_cli.py form-curl 0           # curl template for form #0
```

All commands accept `--json` for machine-readable output.
Exit codes: 0 = success, 1 = error (message on stderr).
