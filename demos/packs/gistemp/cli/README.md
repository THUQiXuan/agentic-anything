# gistemp CLI

Zero-dependency CLI over the `gistemp` resource pack (generated by
[agentic-anything](https://github.com/THUQiXuan/agentic-anything)).

```bash
python gistemp_cli.py pages
python gistemp_cli.py page <page_id>
python gistemp_cli.py --json search <keywords>
python gistemp_cli.py apis
python gistemp_cli.py fetch /some/path      # live same-origin GET
python gistemp_cli.py form-curl 0           # curl template for form #0
```

All commands accept `--json` for machine-readable output.
Exit codes: 0 = success, 1 = error (message on stderr).
