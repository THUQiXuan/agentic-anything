# incident CLI

Zero-dependency CLI over the `incident` resource pack (generated by
[agentic-anything](https://github.com/THUQiXuan/agentic-anything)).

```bash
python incident_cli.py pages
python incident_cli.py page <page_id>
python incident_cli.py --json search <keywords>
python incident_cli.py apis
python incident_cli.py fetch /some/path      # live same-origin GET
python incident_cli.py form-curl 0           # curl template for form #0
```

All commands accept `--json` for machine-readable output.
Exit codes: 0 = success, 1 = error (message on stderr).
